for city in growth_list:
    # print(city) will print the string representation of the CityGrowth,
    # to access the attributes, use city.attribute (e.g. city.id, city.name, etc.)
    print(city)
    print()